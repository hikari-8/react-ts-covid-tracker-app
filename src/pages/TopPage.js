import { Title } from '../components/Title';
import { Selector } from '../components/Selector';

export const TopPage =()=> {
    return(
        <div>
            <Title />
            <Selector />
        </div>
    );
}